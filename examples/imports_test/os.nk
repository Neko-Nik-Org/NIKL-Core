fn get_cwd() -> String {
    return __nikl__.os.get_cwd()
}

let bool_test = False

import "os" as os

fn foo(param_1: Bool) -> String {
    let variable_1 = "1"
    print("Current working directory:", os.get_cwd())

    if param_1 {
        variable_1 = "2"
        print("\nIF -", variable_1, "of type:", type(variable_1))
        const cars = ["Volvo", "BMW", "Ford"]
        // return int(variable_1) + 2
    } else {
        variable_1 = "4"
        print("ELSE -", variable_1, "of type:", type(variable_1))
        // return int(variable_1) + 1
    }

    // print(cars)
    print("X  -", variable_1, type(variable_1), "\n")
    return variable_1
    // This line should not execute
}
