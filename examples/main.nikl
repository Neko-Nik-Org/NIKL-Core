// variable_a: int = 1
// variable_b: bool = True

// Operators
// Arithmetic: + - * / % **
// Comparison: == != < > <= >=
// Logical: && || !
// Bitwise: & | ^ << >>
// Assignment: = += -= *= /= %= **= &= |= ^= <<= >>=


// This is a comment

/* This is a
    multiline comment */

if (some_condition) {
    
}

func some_function(var_a: int, var_b: bool) -> int {
    return var_a + var_b
}