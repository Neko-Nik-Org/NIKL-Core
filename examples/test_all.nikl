// 1 + 2;
/* cat = 19+ 2 * 3 - (4.2 ** 6);

if class

the = "cats are cool
 is true!";

else

func = function() {
    return 1;
}

// != == >= <= < > 
{ } else { }

/* cars = 2; 
ram = 1;
*/

//rats = -3.xwe123;

// cat = 1.2e+35677x if;    // Throws an error

// var neko = 1 + 2;
// print(neko);
// a=nil;

// var cat = 1+2;
// var cat = "cat";
// 1.23e+20;
// 1 / 2;

//var cat= 4 + 1 - 3 / 1 + 3;
// ( (4 + 1) - (3 / 1) ) + 3;
//var cats = 1 / 2;

var result = (2 ** (3 * 4) / 2.123e-13);
const RESTULT = 12345;
// Correct grouping: (2 + ((3 * 4) / 2))
// Currently we only support expressions inside print statements
print(1+2);